package com.nimbusbill.customer.dto; import com.nimbusbill.customer.entity.*; import java.math.BigDecimal; import java.time.*; import java.util.*;
public record PricingPlanResponse(UUID id,String planCode,String planName,String description,List<Version> versions){public record Version(UUID id,int versionNumber,String currency,LocalDate effectiveFrom,LocalDate effectiveTo,PricingVersionStatus status,String createdBy,String approvedBy,Instant createdAt,Instant approvedAt,List<Rule> rules){} public record Rule(UUID id,UUID productId,String productCode,String productName,PricingUnit chargeType,BigDecimal fixedFee,BigDecimal percentageRate,BigDecimal minimumFee,BigDecimal maximumFee,BigDecimal taxRate,int priority,List<Slab> slabs){} public record Slab(UUID id,BigDecimal lowerBound,BigDecimal upperBound,BigDecimal flatFee,BigDecimal percentageRate,int sequenceNo){}}
