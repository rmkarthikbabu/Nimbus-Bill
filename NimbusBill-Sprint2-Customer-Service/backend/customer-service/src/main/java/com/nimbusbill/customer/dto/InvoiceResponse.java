package com.nimbusbill.customer.dto; import java.math.BigDecimal; import java.time.*; import java.util.*;
public record InvoiceResponse(UUID id,UUID billingRunId,UUID customerId,UUID billingAccountId,String invoiceNumber,String status,LocalDate invoiceDate,LocalDate dueDate,String currency,BigDecimal subtotal,BigDecimal taxTotal,BigDecimal adjustmentTotal,BigDecimal grandTotal,Instant createdAt,Instant submittedAt,Instant approvedAt,Instant issuedAt,List<Line> lines){public record Line(UUID id,UUID transactionId,String productCode,String description,BigDecimal baseAmount,BigDecimal taxAmount,BigDecimal totalAmount,String currency){}}
