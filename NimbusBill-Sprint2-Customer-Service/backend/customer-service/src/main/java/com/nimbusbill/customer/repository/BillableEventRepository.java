package com.nimbusbill.customer.repository; import com.nimbusbill.customer.entity.BillableEvent; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface BillableEventRepository extends JpaRepository<BillableEvent,UUID>{boolean existsByTransferIdAndEventType(UUID transferId,String eventType);Optional<BillableEvent> findByTransferIdAndEventType(UUID transferId,String eventType);}
