import {afterEach,describe,expect,it,vi} from 'vitest';
import {assignCustomerPricing,setCustomerProducts} from './customers';
describe('Phase 3 API integrations',()=>{afterEach(()=>vi.unstubAllGlobals());it('sends product limits and billing-account scope',async()=>{const fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify([]),{status:200,headers:{'Content-Type':'application/json'}}));vi.stubGlobal('fetch',fetch);await setCustomerProducts('customer-1',[{productId:'product-1',enabled:true,activationDate:'2026-08-25',expiryDate:null,billingAccountId:'billing-1',transactionLimit:1000,dailyLimit:5000}]);expect(fetch).toHaveBeenCalledWith(expect.stringContaining('/customers/customer-1/products'),expect.objectContaining({method:'PUT',body:expect.stringContaining('billing-1')}));});it('sends scoped pricing assignments',async()=>{const fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify({id:'assignment-1'}),{status:200,headers:{'Content-Type':'application/json'}}));vi.stubGlobal('fetch',fetch);await assignCustomerPricing('customer-1','plan-1','2026-08-25','billing-1','product-1');expect(fetch).toHaveBeenCalledWith(expect.stringContaining('/customers/customer-1/pricing'),expect.objectContaining({body:expect.stringContaining('product-1')}));});});
