import {describe,expect,it,vi} from 'vitest';import {render,screen} from '@testing-library/react';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import BillingOperations from './BillingOperations';vi.mock('../api/customers',()=>({listCustomers:vi.fn().mockResolvedValue({content:[]})}));vi.mock('../api/billing',()=>({listBillingAccounts:vi.fn(),listBillingPeriods:vi.fn(),listBillingRuns:vi.fn(),createBillingAccount:vi.fn(),createBillingPeriod:vi.fn(),previewBillingPeriod:vi.fn(),executeBillingRun:vi.fn()}));describe('BillingOperations',()=>{it('renders Phase 5 billing workflow',()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><BillingOperations/></QueryClientProvider>);expect(screen.getByRole('heading',{name:'Billing operations'})).toBeInTheDocument();expect(screen.getByText(/exactly-once billing runs/i)).toBeInTheDocument();});});
