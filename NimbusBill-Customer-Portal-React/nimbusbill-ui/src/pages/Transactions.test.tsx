import {describe,expect,it,vi} from 'vitest'; import {render,screen} from '@testing-library/react'; import {QueryClient,QueryClientProvider} from '@tanstack/react-query'; import Transactions from './Transactions';
vi.mock('../api/customers',()=>({listCustomers:vi.fn().mockResolvedValue({content:[]})}));vi.mock('../api/transactions',()=>({listTransactions:vi.fn(),createTransaction:vi.fn()}));
describe('Transactions',()=>{it('renders Phase 4 transaction ingestion',()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><Transactions/></QueryClientProvider>);expect(screen.getByRole('heading',{name:'Payment transactions'})).toBeInTheDocument();expect(screen.getByText(/charge a client transaction exactly once/i)).toBeInTheDocument();});});
