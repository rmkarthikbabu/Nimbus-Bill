import {describe,expect,it,vi} from 'vitest';import {render,screen} from '@testing-library/react';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import InternalTransfers from './InternalTransfers';
vi.mock('../api/customers',()=>({listCustomers:vi.fn().mockResolvedValue({content:[]})}));vi.mock('../api/transfers',()=>({listTransferAccounts:vi.fn(),listInternalTransfers:vi.fn(),createTransferAccount:vi.fn(),initiateInternalTransfer:vi.fn()}));
describe('InternalTransfers',()=>{it('renders the parent-transaction workflow',()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><InternalTransfers/></QueryClientProvider>);expect(screen.getByRole('heading',{name:'Internal transfers'})).toBeInTheDocument();expect(screen.getByText(/X → Xa → Ya → Y/)).toBeInTheDocument();});});
